# valid command line flags can be given each per line
address = localhost
    port =              6697      # spaces
user = nobody
group = nobody
password = secret12345
max-channels = 16
client-timeout = 120
logfile = /tmp/gosch.log
selfsigned = true
daemon = false
certfile = /tmp/gosch.pem
#unknown command line arguments are ignored 
unknown-arg = foobar
